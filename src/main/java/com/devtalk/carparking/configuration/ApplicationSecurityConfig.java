package com.devtalk.carparking.configuration;


//@Configuration
//@EnableWebSecurity
public class ApplicationSecurityConfig {

   /* @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
        .authorizeRequests()
        .antMatchers("/parking-management/v1/users")
        .anonymous()
        .and()
        .authorizeRequests()
        .antMatchers("/parking-management/v1/users")
        .permitAll()
        .and()
        .authorizeRequests()
        .anyRequest()
        .authenticated()
        .and()
        .formLogin()
        .and()
        .csrf().disable();
    }*/
}
